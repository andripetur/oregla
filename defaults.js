// default settings of (ó)regla
// TODO : make user editable
var def = {
  scale: "ionian",
  tempo: 80,
  signature: { upper: 4, lower: '4n'},

  fillBufferSettings: {
    offset: 1000,
    length: 20,
    reorder: "none",
    useOldCoords: false,
  },

  quickStartEnabled: true,
}
